language: ruby

branches:
  only:
    - master

sudo: false

rvm:
  - 2.3.1

install: bundle install --jobs=3 --retry=3
cache:
  directories:
  - vendor/bundle

script:
  - bundle exec rake style
  - bundle exec rake spec

notifications:
  disable: true
